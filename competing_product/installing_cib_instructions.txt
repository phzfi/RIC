sudo apt-get install openjdk-8-jdk
sudo apt-get instll Jmagick

Then ./gradlew run

This builds and launch the program. The program is running regardless of the
fact that it says it is still building (Thanks Kristian..)

Make sure you have built the program once while running siege automated tests,
or else the program wont start fast enough for siege (siege waits 20s)
