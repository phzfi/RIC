# RIC
Responsive image cache


### Development environment
```bash
# start virtual machine
vagrant up

# start RIC server
vagrant ssh

cd /vagrant/server

./do_run.sh

# check that script was run
screen -list
```